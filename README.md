Capstone Project

The main loop is in visualizer. You can change the parameters of the box in there.

The physics simulation is run by physics_sim. Currently only uses code from rigid_body, fine_collision, and contacts.
